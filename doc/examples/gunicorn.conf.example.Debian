CONFIG = {
    'mode': 'wsgi',
    'working_dir': '/path/to/servermon',
    'args': (
        '--bind=0.0.0.0:8090',
        '--workers=4',
        '--timeout=10',
        'wsgi:application',
    ),
}
